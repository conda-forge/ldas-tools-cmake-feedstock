schema_version: 1

context:
  name: ldas-tools-cmake
  version: "1.5.0"
  # dependency versions
  igwn_cmake_macros_version: 1.11.0

package:
  name: ${{ name|lower }}
  version: ${{ version }}

source:
  url: http://software.ligo.org/lscsoft/source/${{ name }}-${{ version }}.tar.gz
  sha256: 27687a4ae2c4d4425af2f9742270a2292c1021c5738fdab2dd1b4fe81a0491e3
  patches:
    - boost-prefix.patch

build:
  noarch: generic
  number: 0

requirements:
  build:
    - ${{ compiler('c') }}
    - ${{ compiler('cxx') }}
    - ${{ stdlib('c') }}
    - cmake >=3.2
    - make
    - pkg-config
  host:
    - igwn-cmake-macros >=${{ igwn_cmake_macros_version }}
    - libboost-devel
  run:
    - igwn-cmake-macros >=${{ igwn_cmake_macros_version }}
  ignore_run_exports:
    by_name:
      - ${{ stdlib('c') }}
      - ${{ compiler('cxx') }}

tests:
  - package_contents:
      files:
        - share/ldas-tools/cmake/Modules/LDASToolsCMakeConfig.cmake
  - requirements:
      run:
        - pkg-config
    script:
      - pkg-config --exact-version ${{ version }} ldastoolscmake

about:
  summary: CMake functions used by LDAS Tools
  description: |
    This collection of cmake macros was developed as part of
    the LDAS Tools Suite to have consistency across all of the packages
  homepage: https://wiki.ligo.org/Computing/LDASTools
  repository: https://git.ligo.org/ldastools/LDAS_Tools
  license: GPL-2.0-or-later
  license_file: COPYING

extra:
  recipe-maintainers:
    - duncanmmacleod
    - emaros
